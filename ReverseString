class Solution {
    public void reverseString(char[] s) {
       
        int j=s.length-1; int i=0;
     while(i<j)
       {
           char temp= s[j]; // swap the last and first character
           s[j]=s[i];
           s[i]=temp;
           i++;
           j--; 
       }
    }
}
